package org.robotlegs.utilities.as3bootstrap.common.model
{
	import as3bootstrap.common.IBootstrap;
	
	/**
	 * Interface for the bootstrap model for Robotlegs
	 *
	 * @langversion ActionScript 3.0
	 * @playerversion Flash 9.0.124
	 * 
	 * @author krisrange 
	 */
	public interface IBootstrapModel
	{
		/**
		 * Returns the <code>IBootstrap</code> object
		 * 
		 * @return <code>IBootstrap</code> object 
		 */		
		function get bootstrap():IBootstrap
	}
}